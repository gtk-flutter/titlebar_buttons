name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  window_decorations:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ">=2.4.0 <3.0.0"

flutter:
  uses-material-design: true
